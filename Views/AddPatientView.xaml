<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Hospital_Management_System.Views.AddPatientView"
             xmlns:viewmodels="clr-namespace:Hospital_Management_System.ViewModels"
             Shell.NavBarIsVisible="False"
             Title="Create Appointment">
    <ContentPage.Resources>
        <StyleSheetExtension Source="../Resources/Styles/Style.css" />
    </ContentPage.Resources>
    <StackLayout  Background="Wheat">
        <HorizontalStackLayout VerticalOptions="Start">
            <Border StyleId="patientContainer"  Margin="30,20,30,15">
                <Label Text="Add Patient" StyleId="patientTitle"/>
            </Border>
        </HorizontalStackLayout>
        <VerticalStackLayout Margin="100,10,30,10">
        <VerticalStackLayout HorizontalOptions="Start">

                <HorizontalStackLayout Spacing="10">

                    <VerticalStackLayout>
                    <Label Text="First Name"
                           />
                    <Border>    
                    <Entry Placeholder="First Name"
                           Text="{Binding FirstName}"
                           />
                    </Border>
                </VerticalStackLayout>

                    <VerticalStackLayout>
                        <Label Text="Last Name"
                               Padding="1"
                               TextColor="#C4C4C4"/>
                    <Border>
                        <Entry Placeholder="Last Name"
                               Text="{Binding LastName}"
                        />
                    </Border>
                </VerticalStackLayout>
                    </HorizontalStackLayout>
                <HorizontalStackLayout>
                    <VerticalStackLayout>
                        <Label Text="Date Of Birth"
                             />
                        <Border HorizontalOptions="Start">
                        <DatePicker Date="06/21/2022" 
                                    BackgroundColor="#DBDBDB"/>
                        </Border>
                    </VerticalStackLayout>
                <VerticalStackLayout>
                    <Label Text="Created At"/>
                    <Border HorizontalOptions="Start" Background="#DBDBDB">
                        <DatePicker Date="12/11/2025" />
                    </Border>
                </VerticalStackLayout>
                    </HorizontalStackLayout>

                <VerticalStackLayout>
                <Label Text="Gender"/>
                <Border HorizontalOptions="Start">
                    <Entry  Placeholder="Gender" Text="{Binding Gender}" />
                </Border>
            </VerticalStackLayout>
            <VerticalStackLayout>
                <Label Text="Phone Number"/>
                <Border HorizontalOptions="Start">
                    <Entry Placeholder="Phone Number" Text="{Binding Phone}" />
               </Border>
            </VerticalStackLayout>
                <VerticalStackLayout>
                    <Label Text="Email"/>
                    <Border HorizontalOptions="Start">
                        <Entry Placeholder="Email" Text="{Binding Email}" />
                    </Border>
                </VerticalStackLayout>
                <VerticalStackLayout>
                <Label Text="Blood Group"/>
                <Border HorizontalOptions="Start">
                    <Entry Placeholder="Blood Group" Text="{Binding BloodGroup}" />
                </Border>
            </VerticalStackLayout>
            
            <HorizontalStackLayout Spacing="20">
                <VerticalStackLayout>
                    <Label Text="Emergency Contact Name"/>
                    <Border>
                        <Entry Placeholder="Emergency Contact Name" Text="{Binding EmergencyContactName}" />
                    </Border>
                </VerticalStackLayout>
                <VerticalStackLayout>
                    <Label Text="Emergency Contact Name"/>
                    <Border>
                        <Entry Placeholder="Emergency Contact Phone" Text="{Binding EmergencyContactPhone}" />
                    </Border>
                </VerticalStackLayout>
            </HorizontalStackLayout>



                <HorizontalStackLayout Margin="20" HorizontalOptions="End">
            <!-- BUTTON -->
                <Button Text="Add Patient"
                        
                        Command="{Binding AddPatientCommand}" />

                <!-- Status message -->
                <Label Text="Status"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="Green"/>
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </StackLayout>
</ContentPage>